<h1>Edite o seu livro:</h1>
<form class="form" action="/books/updatebook" method="POST">
  <input type="hidden" name="id" value="{{book.id}}">
  <div class="form-control">
    <label for="title">Título:</label>
    <input type="text" name="title" placeholder="Digite o título:" value="{{book.title}}">
  </div>
  <div class="form-control">
    <label for="title">Autor:</label>
    <input type="text" name="author" placeholder="Digite o nome do autor:" value={{book.author}}>
  </div>
  <div class="form-control">
    <label for="title">Páginas:</label>
    <input type="number" name="pages" placeholder="Digite o número de páginas:" value="{{book.pages}}">
  </div>
  <div class="form-control">
    <label for="title">Editora:</label>
    <input type="text" name="publisher" placeholder="Digite a editora:" value="{{book.publisher}}">
  </div>
  <div class="form-control">
    <label for="title">Ano:</label>
    <input type="number" name="release_year" placeholder="Digite o ano:" value="{{book.release_year}}">
  </div>
  <input type="submit" value="Cadastrar">
</form>